[project]
name = "options-analytics-platform"
version = "0.1.0"
description = "Backend foundation for NIFTY and BANKNIFTY options analytics."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
